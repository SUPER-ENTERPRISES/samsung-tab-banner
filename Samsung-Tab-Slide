<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Samsung Galaxy A8 Tab - Refurbished</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            overflow-x: hidden;
        }

        .banner {
            position: relative;
            width: 100%;
            height: 600px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            overflow: hidden;
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 0 80px;
        }

        .banner::before {
            content: '';
            position: absolute;
            width: 500px;
            height: 500px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 50%;
            top: -150px;
            right: -100px;
            animation: float 6s ease-in-out infinite;
        }

        .banner::after {
            content: '';
            position: absolute;
            width: 300px;
            height: 300px;
            background: rgba(255, 255, 255, 0.08);
            border-radius: 50%;
            bottom: -100px;
            left: -50px;
            animation: float 8s ease-in-out infinite reverse;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(-30px); }
        }

        .content {
            position: relative;
            z-index: 2;
            max-width: 550px;
            animation: slideInLeft 1s ease-out;
        }

        @keyframes slideInLeft {
            from {
                opacity: 0;
                transform: translateX(-50px);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        .badge {
            display: inline-block;
            background: rgba(255, 255, 255, 0.2);
            backdrop-filter: blur(10px);
            color: #fff;
            padding: 8px 20px;
            border-radius: 50px;
            font-size: 14px;
            font-weight: 600;
            margin-bottom: 20px;
            border: 1px solid rgba(255, 255, 255, 0.3);
            animation: pulse 2s ease-in-out infinite;
        }

        @keyframes pulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.05); }
        }

        h1 {
            color: #fff;
            font-size: 56px;
            font-weight: 700;
            margin-bottom: 15px;
            line-height: 1.2;
        }

        .subtitle {
            color: rgba(255, 255, 255, 0.9);
            font-size: 24px;
            margin-bottom: 25px;
            font-weight: 300;
        }

        .features {
            display: flex;
            gap: 30px;
            margin-bottom: 35px;
        }

        .feature {
            color: #fff;
        }

        .feature-icon {
            font-size: 28px;
            margin-bottom: 5px;
        }

        .feature-text {
            font-size: 14px;
            opacity: 0.9;
        }

        .cta-group {
            display: flex;
            gap: 15px;
            align-items: center;
        }

        .btn {
            padding: 16px 40px;
            border: none;
            border-radius: 50px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-block;
        }

        .btn-primary {
            background: #fff;
            color: #667eea;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
        }

        .btn-primary:hover {
            transform: translateY(-3px);
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.3);
        }

        .btn-secondary {
            background: transparent;
            color: #fff;
            border: 2px solid #fff;
        }

        .btn-secondary:hover {
            background: rgba(255, 255, 255, 0.1);
            transform: translateY(-3px);
        }

        .price {
            color: #fff;
            font-size: 18px;
            margin-top: 20px;
        }

        .price-amount {
            font-size: 36px;
            font-weight: 700;
            margin-left: 10px;
        }

        .product-image {
            position: relative;
            z-index: 2;
            animation: slideInRight 1s ease-out;
        }

        @keyframes slideInRight {
            from {
                opacity: 0;
                transform: translateX(50px);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        .tablet-mockup {
            width: 500px;
            height: 500px;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            animation: floatDevice 3s ease-in-out infinite;
        }

        @keyframes floatDevice {
            0%, 100% { transform: translateY(0px) rotate(-2deg); }
            50% { transform: translateY(-20px) rotate(2deg); }
        }

        .tablet-mockup img {
            width: 100%;
            height: auto;
            filter: drop-shadow(0 30px 60px rgba(0, 0, 0, 0.3));
            transition: transform 0.3s ease;
        }

        .tablet-mockup:hover img {
            transform: scale(1.05);
        }

        .refurbished-badge {
            position: absolute;
            top: 20px;
            right: 20px;
            background: #00d4aa;
            color: #fff;
            padding: 10px 20px;
            border-radius: 50px;
            font-size: 12px;
            font-weight: 700;
            text-transform: uppercase;
            box-shadow: 0 5px 15px rgba(0, 212, 170, 0.4);
        }

        @media (max-width: 1024px) {
            .banner {
                padding: 40px;
                height: auto;
                flex-direction: column;
                text-align: center;
                padding: 60px 20px;
            }

            .content {
                max-width: 100%;
            }

            h1 {
                font-size: 42px;
            }

            .tablet-mockup {
                margin-top: 40px;
                width: 350px;
                height: 400px;
            }

            .features {
                justify-content: center;
            }

            .cta-group {
                justify-content: center;
                flex-wrap: wrap;
            }
        }
    </style>
</head>
<body>
    <div class="banner">
        <div class="content">
            <div class="badge">✨ New Launch</div>
            <h1>Samsung Galaxy<br>A8 Tab</h1>
            <div class="subtitle">Premium Performance, Refurbished Excellence</div>
            
            <div class="features">
                <div class="feature">
                    <div class="feature-icon">📱</div>
                    <div class="feature-text">26.69 cm Display</div>
                </div>
                <div class="feature">
                    <div class="feature-icon">🔋</div>
                    <div class="feature-text">7,040 mAh Battery</div>
                </div>
                <div class="feature">
                    <div class="feature-icon">🔊</div>
                    <div class="feature-text">Quad Speakers</div>
                </div>
            </div>

            <div class="cta-group">
                <a href="https://www.esuper.in/products/samsung-galaxy-a8-refurbished-4gb-ram-64gb-rom-4g-lte/1041772000078313017" target="_blank" class="btn btn-primary">Shop Now</a>
                <a href="https://www.esuper.in/products/samsung-galaxy-a8-refurbished-4gb-ram-64gb-rom-4g-lte/1041772000078313017" target="_blank" class="btn btn-secondary">Learn More</a>
            </div>

            <div class="price">
                4GB RAM | 64GB ROM | <span class="price-amount">Best Price</span>
            </div>
        </div>

        <div class="product-image">
            <div class="tablet-mockup">
                <div class="refurbished-badge">Certified Refurbished</div>
                <img src="https://images.samsung.com/is/image/samsung/p6pim/hk_en/sm-x200nzaetgy/gallery/hk-en-galaxy-a8-sm-x200-sm-x200nzaetgy-538328610?$684_547_PNG$" alt="Samsung Galaxy Tab A8">
            </div>
        </div>
    </div>
</body>
</html>
